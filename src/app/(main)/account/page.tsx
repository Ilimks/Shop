import styles from './account.module.scss'
import { AccountSection } from '@/widgets/account/AccountSection';

export default function Account() {
  return (
    <main>
       <AccountSection/>
    </main>
  );
}